{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Capability for the main window",
    "windows": [
        "select",
        "config"
    ],
    "permissions": [
        "core:default",
        "shell:allow-open"
    ]
}
